# Running DB+backend in docker-compose containers
- Generate a custom image based on your backend project:

Run Dockerfile in the project directory

> docker build -t t2-schedule-api:latest .


- Run docker-compose.yaml:

> docker-compose up

or

> docker compose up

- Services are ready to use.

